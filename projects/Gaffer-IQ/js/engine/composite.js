/**
 * js/engine/composite.js
 * Layer: engine (pure). No DOM, no network, no store mutation.
 * Combines all sub-metrics into the composite matchup score (CompositeScore),
 * aggregates scores across a horizon, and projects player value.
 * See FEATURE_ENGINE.md §8–§10. This is the top of the engine call chain.
 */

// TODO(phase-1): implement scoreFixture(fixture, team, ctx),
// scoreOverHorizon(team, horizonKey, ctx),
// scorePlayer(player, horizonKey, ctx),
// rankPlayers(players, horizonKey, ctx)
