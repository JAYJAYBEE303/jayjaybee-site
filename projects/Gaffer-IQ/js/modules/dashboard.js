/**
 * js/modules/dashboard.js
 * Layer: module. Owns the DOM for the GW Decision Dashboard view.
 * Side effects: DOM writes only. Reads from store; horizon-locked to GW1.
 * Renders captaincy pick, suggested XI/bench order, and risk flags for the
 * current GW. Integrates live points via event/<gw>/live/ when in progress.
 * See ARCHITECTURE.md §10 and ROADMAP.md Phase 2C.
 */

// TODO(phase-2): implement renderDashboard(squad), manual squad entry,
// captaincy recommendation, risk flags, live points integration
